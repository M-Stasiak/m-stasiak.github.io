/*const photo = document.querySelector("img");

var reverse = true;
let pom = 0;
function animate() {
    reverse ? pom += 2 : pom -= 2;

    if (pom >= 1000) {
        reverse = false
    }
    if (pom <= 0) {
        reverse = true
    }

    photo.style.margin = pom + 'px';
    requestAnimationFrame(animate); // Wywołaj ponownie funkcję animate
  }

  animate(); // Rozpocznij animację po załadowaniu strony*/